# Included by Fedora nginx in the http context. The default.d location snippet
# uses this value for WebSocket upgrades without forcing Connection: upgrade on
# ordinary HTTP requests.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
