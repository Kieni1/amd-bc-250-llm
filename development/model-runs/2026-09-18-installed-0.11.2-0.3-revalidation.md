# Installed 0.11.2-0.3 appliance revalidation — 2026-09-18

Evidence class: REAL BC-250 + authenticated Open WebUI.

Installed package:

`bc250-llm-server-0.11.2-0.3.fc44.x86_64`

Revalidation run:

`20260918T114329+0200-8855169b`

Evidence archive:

`20260918T114329+0200-8855169b-bc250-revalidation-results.tar.gz`

SHA-256:

`c1336e4d532911315947e5c8a482350c4defc5bb394dadf6c0e986882c7f8637`

## Observed

- install verification: 49 ok / 0 warn / 0 fail;
- normal topology: main/task/embedding active, exclusive agent stopped;
- live CU routing: 40/40 with no off/problem cells;
- Mesa: 26.2.2-6.fc44;
- revalidation harness v4.0 completed with infrastructure PASS, restoration PASS and full coverage;
- embeddings: PASS;
- task: 5/6 automatic, with only `title-fr` marked relevance-fail;
- direct production Translate-Gemma canonical translation: 8/8 PASS;
- direct RAG quality: 4/4 PASS;
- production use-case screen: PASS;
- agent: 2/3, with the Bash case failing only the raw-output format contract because the otherwise valid script was wrapped in Markdown fences;
- authenticated packaged Open WebUI RAG: 3/3 PASS.

The task failure was an evaluator vocabulary gap: the returned title was exactly
`{"title": "Traduction réglementaire"}`. The adjective `réglementaire` was not in the
fixture's regulation semantic group even though the title is relevant. The current source
adds that lexical equivalent rather than changing the task model or prompt.

The agent failure remains a real behavioral caveat. Ornith returned valid Bash but wrapped
it in a Markdown code fence, and the case took about 69.6 seconds with substantial hidden
reasoning. Agent resources/topology worked, but agent product qualification remains open.

## Authenticated production translation smoke

After the revalidation run, the two live production Open WebUI role IDs were exercised
directly through `/api/chat/completions` with the protected administrator key:

```text
DE -> FR
source: Bitte senden Sie das Dokument REF-81 bis zum 12. Oktober 2026 zurück.
result: Veuillez retourner le document REF-81 au plus tard le 12 octobre 2026.

FR -> DE
source: Veuillez retourner le document REF-82 avant le 15 octobre 2026.
result: Bitte geben Sie das Dokument REF-82 vor dem 15. Oktober 2026 zurück.
```

Both directions were correct, identifiers were preserved, dates remained semantically
correct, and authenticated `bc250-openwebui-setup status` reported no package-owned drift.
This proves the production direction Filter executes on the live Open WebUI path, but it is
a smoke check rather than a byte-for-byte rerun of the external Stage-2E hard corpus.

## Follow-up incorporated in 0.11.2-0.4 source

- install now ensures every base model behind an active package-owned Open WebUI role,
  plus the task and embedding defaults, before Open WebUI setup;
- `bc250-verify` checks every active Open WebUI role base against the main Ollama registry;
- `bc250-openwebui-setup status --verbose` prints the verified role/task/RAG/Function contract;
- direct revalidation translation uses an explicit 2048-token budget;
- authenticated revalidation adds an `owui-translation` canonical screen through the real
  production role IDs, without reconstructing the package Filter or system prompt;
- the French title evaluator accepts `réglementaire`/`reglementaire` as the regulation concept;
- completed mixed-quality dashboards surface canonical failure causes instead of only the
  last six step outcomes.

RAG remains plumbing/product-path qualified only on packaged synthetic fixtures; broader
real office documents/Tika/OCR workflows remain future evidence. Agent qualification also
remains open beyond the small static contract.
