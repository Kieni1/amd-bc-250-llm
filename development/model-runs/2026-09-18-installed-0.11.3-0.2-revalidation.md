# Installed 0.11.3-0.2 revalidation — 2026-09-18

Installed package: `bc250-llm-server-0.11.3-0.2.fc44.x86_64`

Kernel: `7.2.5-200.fc44.x86_64`

Harness: `4.2`

Run ID: `20260918T214521+0200-c4053fd0`

Evidence bundle SHA-256:
`42713590e9ce58cfd0c499cc0013e98a6de3ed69843917bc169053c25bb389a1`

## Result

The package installed cleanly and `bc250-install` verification completed with `54 ok / 0 warn / 0 fail`.
The subsequent full authenticated revalidation completed in about 14 minutes with:

```text
Run state       COMPLETED
Infrastructure  PASS
Quality         MIXED       7 pass / 1 quality-fail / 0 skipped
Restoration     PASS
Coverage        FULL
```

The single quality failure remained the canonical task `tags-de` case at `5/6` because the task model emitted two JSON objects instead of the required single object. The corrected evaluator attributed only `format-contract`; it did not cascade the parse failure into language/relevance failures.

The v4.2 diagnostic path also behaved as intended. The dedicated GPT-OSS/Jina edge stage observed bounded context truncation from `8662 -> 8320` prompt-evaluated tokens and retained PASS because the observation stayed within the existing severe-truncation policy.

Open WebUI package-owned state remained drift-free; canonical Open WebUI translation and RAG checks passed; final restoration/health passed.

## Interpretation

This is real installed-device evidence for `0.11.3-0.2`. It qualifies the 0.11.3 model-manager/revalidation-v4.2 package line on this device, but it does not qualify later source changes automatically. In particular, the 0.11.3-0.3 tag-prompt change still requires its own focused installed-device task recheck.
