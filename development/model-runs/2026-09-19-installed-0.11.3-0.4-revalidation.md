# Installed 0.11.3-0.4 revalidation — 2026-09-19

This is exact real-device evidence from the refined `0.11.3-0.4` package line. Preserve the
installed release number when later source advances; do not relabel this run as 0.11.3-0.5.

## Installed/runtime identity

```text
Package:  bc250-llm-server-0.11.3-0.4.fc44.x86_64
Harness:  v4.2
Kernel:   7.2.5-200.fc44.x86_64
Run ID:   20260919T103026+0200-0268b429
```

The preceding guided installer run used the refined 0.4 model-pass UX: required production,
task and embedding models summarized as current; the optional picker showed concise current
rows and deferred agent rows; disabled MTP models were not selectable through the generic
picker. Core `bc250-verify --summary` reported **54 ok / 0 warn / 0 fail**.

The RPM artifact SHA was not captured in the supplied transcript, so this record does not
invent one.

## Whole-appliance result

```text
Run state       COMPLETED
Infrastructure  PASS
Quality         PASS       8 pass / 0 quality-fail / 0 skipped
Restoration     PASS
Coverage        FULL
Elapsed         00:13:56
```

Canonical qualification summaries:

```text
embeddings             PASS   1/1 qualification cases
 task                   PASS   6/6
translation             PASS   8/8
rag-quality             PASS   4/4
production usecase      PASS   5/5
agent                   PASS   3/3
Open WebUI translation  PASS   8/8
Open WebUI RAG          PASS   3/3
```

The strict task `tags-de` case that previously emitted duplicate JSON passed in this run,
which is evidence that the tightened prompt fixed the observed product-contract failure on
this installed package.

## Important non-failing evidence

The GPT-OSS/Jina edge qualification passed its existing policy but recorded tight resource
headroom:

```text
prod-gpt-oss20b-ggml-org-mxfp4
  decode_tps:             78.59
  minimum context:        16384
  GPU residency ratio:    1.0
  MemAvailable minimum:   193.36 MiB
  temperature maximum:    69 C
  hard MemAvailable floor 128 MiB
```

The same phase recorded non-severe context truncation:

```text
ctx_704-1: prompt tokens 8662 -> 8320
policy: PASS (not severe)
```

Production use-case `office-draft-e2b` also passed its semantic acceptance while returning
`done_reason=length`; its canonical record contains the `output-budget` diagnostic. This is
not a quality failure, but it is useful operator evidence and motivated the following 0.5
source iteration to surface such accepted budget exhaustion in the top-level Diagnostics
section.

## Evidence artifact

Supplied bundle:

```text
20260919T103026+0200-0268b429-bc250-revalidation-results.tar.gz
SHA-256: 8cdf52fb18336b711ecaf4b6118517bd13642a3f450aeba05b8ea0c3c5db7c5f
```

## Interpretation

This run is the newest complete real-device appliance qualification at the time of the
0.11.3-0.5 source work. It is valid historical evidence for exact installed 0.11.3-0.4,
not automatic qualification of later source. The 0.5 changes are installer optional-setup
UX/post-checks and diagnostic visibility; they deliberately do not relax the resource,
quality, GPU/device-error or restoration gates proven here.
