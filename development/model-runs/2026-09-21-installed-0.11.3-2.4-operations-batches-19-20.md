# Installed 0.11.3-2.4 operations acceptance — Batches 19–20

## Evidence class

Operator-supplied device acceptance for exact installed:

```text
bc250-llm-server-0.11.3-2.4.fc44.x86_64
```

This record preserves the final incremental Operations handover. The raw acceptance archive was not
independently inspected in the source integration environment, so this is not reclassified as
independent source-side verification.

## Batch 19 — operational hygiene / storage / residue

Result: **PASS**. The audit was observational and non-destructive: no service restart, configuration
change, prune, maintenance mutation or restore was performed.

Reported final state included:

- root filesystem about 60% used with about 372 GB free and about 1% inode use;
- backup retention exactly 7 configuration and 14 identity backups;
- zero zombies, relevant deleted-open-file handles, relevant core dumps and package-owned
  world-writable paths;
- zero critical service-log or new GPU/OOM/kernel failure signatures;
- monitored active services at `NRestarts=0`, normal topology, live CU routing `40/40`, zero failed
  units and clean `rpm -V`;
- verifier `53 OK / 1 WARN / 0 FAIL`, with the single warning already attributed to the separate OWUI
  model desired-state verifier defect;
- no uncontrolled storage growth. `/var/lib/bc250-llm-server` was large because expected model/runtime
  data and configured swap dominate it, while filesystem `df` still showed healthy headroom;
- remaining `/tmp` artifacts belonged to acceptance harnesses, not normal package runtime;
- `/run/lock/bc250-llm-maintenance.lock` was a normal zero-byte `flock` anchor, not stale PID state.

No non-OWUI source defect was identified.

## Batch 20 — supported reboot reconstruction

Result: **PASS** using the supported operator command:

```bash
sudo reboot
```

The known-problematic `sudo systemctl reboot` path was deliberately not exercised.

Reported post-boot state included:

- changed boot ID and exact 2.4 NVR preserved;
- main/task/embedding lanes, nginx/office endpoint and Open WebUI reconstructed without manual repair;
- agent lane remained intentionally inactive in normal mode;
- live CU routing restored to `40/40`;
- service enabled/active state and maintenance timer policy survived reboot;
- maintenance configuration hash unchanged;
- monitored services at `NRestarts=0`, zero failed units, clean `rpm -V` and no new
  OOM/panic/GPU-reset/device-loss/VM-fault signature;
- final verifier `53 OK / 1 WARN / 0 FAIL`, again solely the separately owned OWUI status defect.

This closes supported reboot persistence for exact installed 2.4.

## Deferred anomaly

Historical `sudo systemctl reboot` attempts remain associated with a subsequent unusable/crashed boot,
while `sudo reboot` is repeatedly reliable. No root cause is assigned. Package-owned reboot paths are
already corrected to the supported invocation, so deeper diagnosis is a separate, explicitly deferred
track and is not a 2.4 release blocker.

## Operations conclusion

Broad non-OWUI device acceptance for exact installed 2.4 is effectively complete. Further endurance
or naturally-fired-timer work is optional robustness testing unless new evidence appears. Batches
19–20 require no source change.
