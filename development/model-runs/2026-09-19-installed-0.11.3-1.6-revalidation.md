# Exact installed 0.11.3-1.6 revalidation — 2026-09-19

Exact installed package:

`bc250-llm-server-0.11.3-1.6.fc44.x86_64`

Run identity:

- run: `20260919T223602+0200-df0d9c87`;
- harness: v4.2;
- kernel: `7.2.5-200.fc44.x86_64`;
- guided installer/core verifier: **54 ok / 0 warn / 0 fail**;
- revalidation run state: **COMPLETED**;
- infrastructure: **PASS**;
- restoration: **PASS**;
- coverage: **FULL**;
- quality summary as scored by the then-current fixture: **7 pass / 1 quality-fail / 0 skipped**.

## Important closure result

This run closes the earlier Jina restoration defect. Direct RAG quality completed **4/4 PASS**, the
embedding residency set restored successfully, package-owned Open WebUI state remained unchanged,
and final appliance health passed. The non-empty embedding reload probe therefore works on the real
Ollama 0.34 appliance path.

## Task `tags-en` adjudication

The only scored quality failure was `tags-en: relevance` (5/6 task cases). The actual strict JSON
response was:

```json
{"tags": ["Document Analysis", "Text Recognition", "Multilingual Data"]}
```

for a conversation about local OCR models, scanned invoices, tables and multilingual office
documents. The fixture credited literal OCR/invoice/document/table groups; `Document Analysis`
matched the document group but `Text Recognition` was not accepted as an OCR synonym. Current source
therefore treats this as an **evaluator vocabulary gap**, not a task-model/prompt failure, and adds
`text recognition` / `optical character recognition` to the existing OCR semantic group without
lowering the two-group threshold or otherwise weakening relevance acceptance.

That source-side evaluator correction still needs an exact-current-package device rerun before it is
hardware-qualified as 6/6.

## GPT-OSS + Jina edge evidence

The current worst-case production coexistence path remained within appliance policy:

- GPT-OSS decode mean: **79.293 tok/s**;
- minimum `MemAvailable`: **156.266 MiB**;
- diagnostic tight-headroom threshold: 512 MiB;
- hard safety floor: 128 MiB;
- swap start / peak / end: 994.879 / 1011.945 / 1005.000 MiB;
- `swap_peak_delta_mib`: **17.066 MiB**;
- maximum temperature: **72 C**;
- serious kernel/GPU device errors: none in the qualification result;
- context diagnostic: 8662 input/prompt tokens -> 8320 evaluated prompt tokens, policy PASS as
  non-severe truncation.

Interpretation: GPT-OSS remains a valid production deep-reasoning / memory-edge reference, but this
run tightens the demonstrated minimum headroom from the earlier ~193 MiB case to ~156 MiB. Do not
raise or weaken the 128 MiB hard floor from this result. Continue to distinguish allocated context
from actually demonstrated untruncated prompt length.

## Release interpretation

This is exact-1.6 device evidence. It does **not** qualify later 1.7 source changes. Release 1.7 keeps
model bytes, runtime topology, hard resource thresholds and RAG roles unchanged, but its new
status/credential/coding/MTP lifecycle changes still require their own installed-device checks where
applicable.
