# Installed 0.11.3-1.7 full revalidation — 2026-09-20

## Evidence boundary

Installed package: `bc250-llm-server-0.11.3-1.7.fc44.x86_64`.
Run ID: `20260920T001247+0200-e1c7869c` with revalidation harness v4.2.
This is exact installed-1.7 BC-250 evidence. It does not qualify later source releases.

## Install / core verification

The package was upgraded from exact `0.11.3-1.6.fc44`. The guided installer reported:

- Ollama 0.34.0 current;
- kernel current;
- TTM profile active;
- swap configured;
- 40-CU support prepared and live routing healthy at 40/40;
- 373 GiB storage headroom;
- normal runtime topology established;
- production 5/5, task 1/1 and embedding 1/1 required models current;
- standalone MTP inventory visible but not selectable/fetched by installer convergence;
- Open WebUI package baseline applied and verified with no desired-state drift;
- core verification: **54 OK / 0 warn / 0 fail**;
- optional maintenance setup was skipped, preserving existing configuration.

The final installer summary correctly separated core success from application desired state:

```text
Core installation and verification completed successfully.
Open WebUI baseline: APPLIED + VERIFIED
```

## Whole-appliance revalidation

Final result:

```text
Infrastructure: pass
Quality:        pass
Restoration:    pass
Coverage:       full
Quality steps:  8 pass / 0 quality-fail / 0 skipped
```

Recent final stages included Open WebUI translation, Open WebUI RAG,
Open WebUI state-unchanged, restoration, final-health and qualification-complete PASS.
This closes the exact-1.6 task-evaluator false negative after release 1.7 added the narrow
`Text Recognition`/OCR synonym correction.

## GPT-OSS / Jina edge diagnostic

The dedicated edge stage passed policy while recording tight resource headroom:

- prompt truncation diagnostic: `8662 -> 8320` prompt tokens on `ctx_704-1`, accepted as non-severe;
- minimum MemAvailable: **167 MiB**;
- diagnostic threshold: 512 MiB;
- hard floor: 128 MiB;
- policy result: PASS.

This does not establish clean 16K real-prompt execution; allocated/configured context and
demonstrated prompt length remain distinct evidence.

## Interpretation

Exact installed 1.7 is the newest fully qualified whole-appliance release in this evidence set:
installer/core verification clean, infrastructure PASS, quality 8/8, restoration PASS and full
coverage. Later 1.8 source work is driven by the separate exact-1.7 support/maintenance campaign;
that campaign found safe-power and 40-CU status return-code defects and therefore does not weaken
the completed 1.7 model/application revalidation result.

See `2026-09-20-installed-0.11.3-1.7-support-maintenance.md` for that separate support campaign.
